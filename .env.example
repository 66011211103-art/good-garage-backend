# คัดลอกไฟล์นี้เป็นชื่อ .env แล้วใส่ค่าจริงของเครื่องคุณ
PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=garage_app

# เปลี่ยนเป็นข้อความลับยาวๆ แบบสุ่ม ห้ามใช้ค่านี้จริงตอน production
JWT_SECRET=change_this_to_a_long_random_secret_key
